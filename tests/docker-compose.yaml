services:
  brainz-test:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      - ../backend:/app
      - ./:/tests
      - ./data:/tests/data
    environment:
      - DATA_DIR=/tests/data
      - API_KEYS=
    command: python -m pytest /tests/ -q --tb=short
